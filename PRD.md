# PRD: 채용 리포트 자동 생성 에이전트 (Recruiting Report Agent)

- 문서 버전: v0.6 (재개 기능 구현·검증, 설정 비교 실험 추가)
- 작성일: 2026-09-09
- 작성자: risa02076

---

## 0. 한 줄 요약

리크루터가 매주/매월 스프레드시트에서 수동으로 집계하던 채용 현황(공고별 지원자 수, 단계별 이탈, 소요 시간, 소싱 채널 효과)을,
에이전트가 스스로 "이번 기간에 뭐가 이상한지"를 찾아 원인을 추가로 조사하고 서술형 인사이트를 만들어주는 서비스.

숫자(판단 없는 고정 집계)는 **우리 앱의 "핵심지표" 화면**이 DB를 직접 조회해서 매번 표로 보여주고,
글(판단이 들어간 원인 분석·다음 조치 제안)은 같은 앱이 자체 DB에 초안으로 저장했다가 리크루터가 검토·승인한 뒤에만
확정된 것으로 노출한다. 에이전트는 자동 발송하지 않으며, 판단 결과물에 대한 승인 권한 자체가 에이전트에게 없다.

> v0.4까지는 숫자 쪽을 Looker Studio(Google Sheets 연동)로 시각화할 계획이었으나, 그러려면 사용자가
> 스프레드시트·서비스 계정·대시보드 레이아웃을 전부 손으로 만들어야 했다 — 에이전트 동작과 무관한
> 순수 수작업이라 v0.5에서 필수 경로에서 뺐다(3.1 참고). `generate_report_tabs.py`/`sheets_writer.py`는
> Sheets 연동을 원하면 여전히 쓸 수 있는 선택지로 남아있다.

---

## 1. 문제 정의

**현재 문제**
- 리크루터는 매주/매월 채용 퍼널 데이터(지원 → 서류합격 → 1차면접 → 2차면접 → 오퍼 → 입사)를 스프레드시트에서 손으로 취합해 보고서를 작성한다.
- 단순 집계(이번 달 지원자 수 몇 명)는 기계적이지만, "왜 오퍼 수락률이 떨어졌는지" 같은 원인 분석은 항상 사람이 데이터를 이리저리 잘라보며 찾아야 해서 시간이 오래 걸린다.
- 보고서 포맷과 톤이 작성자마다 달라 채용 리더십이 매번 다른 방식의 보고서를 받는다.

**에이전트가 대신 처리할 것**
- **입력**: 조회 기간(예: "2026년 8월"), 대상 범위(전체 / 특정 부서·직무 필터, 선택)
- **처리**: 기간 내 채용 데이터를 DB에서 조회 → 관리도 기반으로 주목할 만한 지표(이상치) 탐지 → 필요하면 원인 파악을 위한 추가 조회를 스스로 계획·실행 → 서술형 인사이트 및 다음 조치 제안을 우리 DB에 **초안(draft)**으로 저장
- **출력(끝)**: 우리 앱의 리포트 미리보기 화면(초안 인사이트 텍스트 + 핵심지표 화면 링크) + 실행 과정 로그. 리크루터가 승인해야만 초안이 **확정(approved)**으로 바뀌어 최종 리포트로 노출된다. (참고: 숫자는 판단이 없는 고정 집계라 앱의 핵심지표 화면이 DB를 매번 다시 조회해서 항상 최신 상태로 보여준다 — 3.1 참고)

**끝나는 조건(Definition of Done)**: 사람이 초안을 검토하고 승인 버튼을 누른 시점. 에이전트가 "다 했다"고 말하는 시점이 아니다.

---

## 2. 타겟 유저

| 유저 | 역할 | 니즈 |
|---|---|---|
| 리크루터 (1차 사용자) | 리포트 생성을 트리거하고, 초안을 검토·수정·승인 | 반복 집계 작업 시간 단축, 원인 분석 부담 감소 |
| 채용 리더십 (리포트 소비자) | 승인된 최종 리포트를 열람 | 일관된 포맷, 신뢰 가능한 숫자, 핵심만 요약된 인사이트 |
| (운영/어드민, 선택) | 데이터 커넥션·권한 관리 | 어떤 쿼리가 실행됐는지 감사(audit) 가능해야 함 |

---

## 3. 데이터 스키마 (집계값만, PII 없음)

개인 식별 정보(지원자 이름, 연락처, 이력서 등)는 애초에 스키마에 포함하지 않는다. 에이전트가 다루는 모든 데이터는 이미 집계된 숫자다.

> 아래 enum 값들은 사용자가 제공한 참고 사례(사내 "Data Driven Recruiting Dashboard")의 실제 항목명을 그대로 반영했다.
> 회사마다 부서명·채용경로명이 다르므로 실제 연동 시 `dim_department`, `dim_channel` 테이블로 빼서 설정 가능하게 한다(하드코딩 금지).

```
job_postings(
  id, title,
  department,       -- enum: Engineering | Business | IT/DATA | Management Control | P&C
  channel_default,
  opened_date, closed_date
)

-- 퍼널 단계 이벤트: 단계별 인원 스냅샷 (참고 사례의 퍼널 바 그대로)
funnel_events(
  job_posting_id, period, stage, count
  -- stage ∈ {application_stage, sc_stage, interview_stage, reference_check, offer_sent, offer_signed}
  --   application_stage = 서류 접수, sc_stage = 서류전형(Screening) 통과,
  --   interview_stage = 면접(1차+2차 통합 or interview_1/interview_2로 분리 가능),
  --   reference_check = 평판조회, offer_sent = 오퍼 발송, offer_signed = 오퍼 수락(입사확정)
)

-- 상태별 현황 (KPI 카드: Total Applicant / Self-Drop / Rejected / In-Progress)
applicant_status_counts(
  job_posting_id, period,
  status  -- enum: total_applicant | self_drop | rejected | in_progress
  count
)

-- 진행 중 지원자의 현재 단계 분포 (In-Progress Ratio 도넛)
in_progress_stage_ratio(
  job_posting_id, period, stage, ratio
  -- stage ∈ {application_review, screening_test, 1st_interview, 2nd_interview, offer_sent, offer_signed}
)

-- 소싱 채널별 지표 (전체 지원자 "지원경로" 도넛 + 합격자만의 "합격자 지원경로" 도넛)
channel_stats(
  job_posting_id, period,
  channel,   -- enum: 직접지원 | 사내추천 | 지인추천 | Direct Sourcing | 산학협력 | Lab
  scope,     -- enum: all_applicants | hired_only  (두 도넛을 구분하는 필드)
  count
)

-- 부서별 지원자 분포 ("Total Applicant Analytics" 도넛)
department_stats(period, department, applicant_count)

-- 채용 소요기간 (AVG Date to Join / AVG date to hire)
stage_duration(job_posting_id, department, metric, avg_days, period)
  -- metric ∈ {date_to_join, date_to_hire}

-- YTD 누적 지표 (하단 6개 KPI 카드: Joined/En Joined/Hired/En Hired/AVG Date to Join/AVG date to hire)
ytd_summary(
  year, department_scope,  -- department_scope: "all" | 특정 부서(예: "Engineering" → 참고사례의 "En " 접두어)
  joined_count, hired_count, avg_date_to_join, avg_date_to_hire
)
```

**참고 사례와 스키마 매핑 메모**
- 참고 대시보드의 "En Joined(YTD)", "En Hired(YTD)"는 특정 부서(Engineering으로 추정)만 필터링한 값 — `ytd_summary.department_scope`로 표현.
- "In-Progress Ratio" 도넛은 `applicant_status_counts.status = in_progress`인 인원을, 다시 현재 멈춰있는 단계별로 쪼갠 것 — `in_progress_stage_ratio`.
- "지원경로" vs "합격자 지원경로"는 같은 채널 축을 모집단만 다르게(`scope`) 자른 것 — 쿼리 도구가 이 둘을 같은 파라미터로 다루도록 설계.

### 3.1 핵심지표 화면 (판단 없는 숫자만) — Google Sheets/Looker Studio는 선택 사항으로 격하

핵심지표/세부지표/연간누적지표는 전부 고정된 집계 쿼리 결과라 **판단이 필요 없다**. 그래서 에이전트 도구가
아니라 `app/generate_report_tabs.py`의 `build_kpi_rows()` / `build_detail_rows()` / `build_ytd_rows()`
(LLM을 전혀 쓰지 않는 순수 조회 함수)가 DB를 직접 계산하고, 우리 앱의 **`/metrics`(핵심지표) 화면**이 이
함수들을 그대로 호출해서 표로 보여준다. 판단이 들어가는 유일한 결과물인 인사이트는 여기 오지 않는다
(3.2 참고) — 그래서 승인 전/후를 나누는 이유 자체가 없고, 요청마다 DB에서 다시 계산하므로 항상 최신이다.

```
build_kpi_rows()      -> period, total_applicant, self_drop, rejected, in_progress (24개월치)
build_detail_rows()   -> period, category, item, scope, value
                          -- category ∈ {funnel, department, channel, in_progress}
                          -- scope는 channel 행에서만 all_applicants|hired_only, 나머지는 빈 값
build_ytd_rows()      -> year, department_scope, joined_count, hired_count, avg_date_to_join, avg_date_to_hire
```

> **변경 이력**: v0.2~v0.4에서는 이 숫자를 Google Sheets(단일 스프레드시트, kpi/detail/ytd 3탭) +
> Looker Studio 대시보드로 시각화할 계획이었다. 그런데 Looker Studio는 대시보드를 코드로 만드는 API가
> 없어서, 결국 사용자가 스프레드시트·서비스 계정·대시보드 레이아웃을 전부 손으로 만들어야 했다 —
> 이건 에이전트가 하는 일이 아니라 순수 수작업이고 이번 과제의 채점 기준(도구/루프/사람개입/관찰가능성/
> 평가)과도 무관하다는 사용자 판단으로, v0.5에서 **핵심지표를 앱 안에서 바로 보여주는 방식**으로 단순화했다.
> `app/sheets_writer.py` + `app/generate_report_tabs.py`의 Google Sheets 쓰기 경로는 코드에 그대로
> 남아있어 원하면 여전히 켤 수 있지만(예: 나중에 정말 도넛/퍼널 차트가 필요해지면), 필수 경로는 아니다.

### 3.2 인사이트 저장소 (우리 앱 DB — 판단이 들어간 유일한 결과물)

에이전트가 판단해서 만든 서술형 인사이트(요약/원인분석/다음조치제안)는 Sheets가 아니라 **우리 앱 자체 DB**의
`insight_drafts` 테이블에 저장한다. 승인 전/후 구분은 이 테이블의 `status` 컬럼(draft → approved)이 담당한다.

```
insight_drafts(run_id, period, summary_text, root_cause_text, next_action_text,
                generated_at, status, approved_at)
  -- status ∈ {draft, approved}
```

- 에이전트 도구(`write_insight_draft`)는 **항상 status='draft'로만** 쓸 수 있다 — approved로 바꾸는 기능 자체가 이 도구에 없다.
- "승인" = 사람이 앱 UI에서 승인 버튼을 눌렀을 때만 서버가 `status`를 `approved`로 바꾸는 것(`app/insight_store.py`의 `approve()`). 이 함수는 에이전트 도구 목록에 노출되지 않는다.
- 리포트 미리보기 화면은 DB의 인사이트 텍스트 + 핵심지표 화면 링크(3.1의 항상-최신 숫자)를 함께 보여준다. 두 데이터 소스가 분리돼 있지만, 화면에서는 하나의 리포트처럼 합쳐 보여준다.

---

## 4. 워크플로 설계

### 4.1 트리거
- 리크루터가 웹 화면에서 기간(및 선택적 필터: 부서/직무)을 선택하고 "리포트 생성" 버튼 클릭.
- (2차 확장) 매월 1일 자동 스케줄 트리거 → 단, 승인 전까지는 사람에게만 노출.

### 4.2 단계별 흐름 — 에이전트 판단 vs 도구 처리 구분

| 단계 | 주체 | 내용 |
|---|---|---|
| 1 | **도구** | DB 조회 도구로 선택된 기간의 기본 퍼널 지표(단계별 인원, 전환율) 조회 |
| 2 | **에이전트 판단** | **관리도(Control Chart) 기반 이상치 탐지**(ADsP 통계 개념 적용, 4.2.1 참고) — 어떤 지표를 주목할지, 원인 규명을 위해 무엇을 더 조회해야 할지 스스로 계획 |
| 3 | **도구** | 에이전트가 세운 계획에 따라 추가 조회 실행 (예: 직무별 breakdown, 소싱 채널별 breakdown, 기간별 추이) |
| 4 | **에이전트 판단** | 추가 데이터로 원인 가설이 설명되는지 재평가. 설명이 부족하면 3단계로 되돌아가 재조회 (최대 반복 횟수 내에서) |
| 5 | **에이전트 판단 + 도구** | 숫자를 표로 보여주는 건 핵심지표 화면이 별도로 맡으므로, 여기서는 코드 실행(샌드박스)으로 관리도 통계값(평균·표준편차·UCL/LCL)만 계산 |
| 6 | **에이전트 판단** | 데이터·통계값을 종합해 서술형 인사이트와 다음 조치 제안 작성 |
| 7 | **도구** | `write_insight_draft` 도구로 우리 DB의 `insight_drafts`에 인사이트를 **status='draft'**로 기록 (3.2 참고) |
| 8 | **사람 개입** | 리크루터가 앱에서 draft 인사이트 + 핵심지표 화면을 함께 검토·수정 요청 또는 승인. **승인 버튼을 눌러야만** 앱 서버가 DB 상태를 `approved`로 변경 — 에이전트에게는 이 상태를 바꿀 권한이 아예 없음 |

> 참고: 핵심지표/세부지표/연간누적지표(Sheets)는 이 8단계 루프와 별개로, `app/generate_report_tabs.py`가
> 독립적인 스케줄(예: 매일 새벽)로 항상 최신 상태를 유지한다 — 판단이 없으니 이 루프를 기다릴 필요가 없다.

### 4.2.1 이상치 탐지 방법 — 관리도(Control Chart) 기반

전월 대비 단순 증감률(%) 대신, 과거 N개 기간(기본 12개월)의 평균(μ)과 표준편차(σ)로 관리 상한/하한(UCL/LCL)을 구하고
이번 기간 값이 그 범위를 벗어날 때만 "이상치"로 판단한다. ADsP에서 다루는 통계적 공정관리(SPC)/정규분포 개념을 그대로 적용한 것.

```
UCL = μ + 2σ   (또는 3σ, 지표 변동성에 따라 조정 가능한 설정값)
LCL = μ - 2σ
이상치 조건: 이번 기간 값 > UCL  또는  값 < LCL
```

- 이 계산은 `run_chart_code`(관리도 통계 계산으로 역할 재정의) 도구가 담당 — 순수 통계 연산이라 코드 실행 샌드박스가 적합.
- 단순 %보다 나은 점: 원래 변동이 큰 지표(예: 표본 수가 적은 소규모 공고)를 과민하게 이상치로 잡지 않고, 정말 통계적으로 유의한 변화만 골라낸다.
- N(기준 기간 수)과 시그마 배수는 설정값으로 빼서, 평가(10장) 단계에서 임계값을 바꿔가며 튜닝할 수 있게 한다.

### 4.3 종료 조건 (무한 루프 방지)
- 최대 반복 조회 횟수: 5회 (3~4단계 루프)
- 실행당 비용 한도: 세션당 토큰 예산 초과 시 현재까지의 결과로 리포트 생성 후 "예산 초과로 조기 종료" 표시
- 시간 제한: 1회 실행 3분 초과 시 타임아웃, 중간 상태 저장 후 사람에게 알림

### 4.4 상태 관리 & 재개

- 각 실행(run)은 `run_id`로 상태를 저장: 현재 단계, 지금까지의 조회 결과, 판단 근거, `insight_drafts`에 기록한 draft 여부.
- **재개는 실제로 구현·검증됨** (`app/agent_loop.py`의 `resume_run()`, 2026-09-09). 매 턴이 끝날 때마다
  OpenAI 대화 상태(`messages`) 전체를 `runs.messages_json`에 스냅샷으로 저장해두고, 프로세스가 죽는 등
  중간에 끊겨도(status='running'인 채로 남음) `resume_run(run_id)`를 부르면 그 스냅샷을 불러와 **처음부터
  다시 하지 않고** 이어서 진행한다. 재조회 횟수는 `runs.query_round_count`에서, draft 작성 여부는
  `insight_drafts`에 해당 run_id 레코드가 있는지로 재구성한다. `tests/test_resume.py`가 실제 API로
  "1턴만 실행 후 강제 중단 → 재개 → 완료"를 검증한다(테스트 전용 `max_iterations` 파라미터로 중단 시점을
  통제, 통과 확인).
- **간소화 지점(정직하게 밝힘)**: nudge_count(도구 호출 없이 끝내려 할 때 재요구한 횟수, 4.2 참고)는
  재개 시 저장해두지 않고 0부터 다시 세고, 시간 제한(3분)도 재개 시점부터 새로 시작한다 — 크래시로
  멈춰있던 시간을 실행 시간에 포함시키는 게 의미 없다고 판단해서다.

---

## 5. 도구(Tool) 계획

| # | 도구명 | 용도 | 입력 스키마 | 출력 스키마 | 권한 |
|---|---|---|---|---|---|
| 1 | `query_recruiting_db` | 채용 집계 데이터 조회 | `{ period: {start, end}, filters?: {department?, job_posting_id?, channel?}, metric: "funnel" \| "status_counts" \| "in_progress_ratio" \| "channel" \| "department" \| "stage_duration" \| "ytd_summary", scope?: "all_applicants" \| "hired_only" }` | `{ rows: [...], row_count }` | **읽기 전용**. 화이트리스트된 쿼리 템플릿만 허용(임의 SQL 실행 금지) — DB 인젝션 및 스코프 이탈 방지 |
| 2 | `run_chart_code` | 코드 실행 샌드박스에서 관리도 통계값(μ, σ, UCL/LCL) 계산 — 표로 보여주는 건 핵심지표 화면이 별도로 담당하므로 순수 통계 연산만 수행 | `{ metric_series: number[], sigma_multiplier?: 2\|3 }` | `{ mean, stddev, ucl, lcl, is_outlier }` | 샌드박스 격리, 네트워크·파일시스템 접근 없음, 실행 시간 제한(예: 10초) |
| 3 | `write_insight_draft` | 우리 DB `insight_drafts`에 인사이트를 **draft로만** 기록 (3.2 참고) | `{ run_id, period, summary_text, root_cause_text, next_action_text, generated_at }` | `{ run_id, status }` | **draft 쓰기만 가능**. approved로 바꾸는 기능이 이 도구에 아예 없음(별도 함수 `approve()`, 에이전트 도구 목록에 없음). 숫자 집계(kpi/detail/ytd)는 이 도구로 쓸 수 없음 — 그건 `app/generate_report_tabs.py`(비-에이전트 스크립트)의 역할 |

**설명(description) 작성 원칙**: 각 도구는 "언제 쓰는지"를 1문장으로 명시한다. 예) `query_recruiting_db`는 "구체적인 숫자가 필요할 때마다 호출하고, 절대 값을 추측하지 않는다"를 시스템 프롬프트에 명시.

**실패 처리 규칙**
- 조회 결과가 비어있음 → 해당 지표는 "데이터 없음"으로 명시, 조회 재시도 1회 후 포기
- 도구 호출 타임아웃/에러 → 최대 2회 재시도(backoff), 이후 실패 시 해당 단계를 건너뛰고 "이 지표는 조회 실패" 주석 남김 + 실행 로그에 에러 기록
- 관리도 통계 계산 실패(표본 부족 등) → 이상치 판단 없이 원값만 기록, 리포트 자동 중단하지 않음
- DB 쓰기 실패 → 사람에게 즉시 알림, 인사이트 텍스트는 화면에 그대로 노출(작업 유실 방지) — 재시도 1회 후에도 실패하면 실행 로그에 에러로 남기고 중단

**위험한 도구의 권한 최소화**
- 데이터를 수정/삭제하는 도구는 아예 두지 않는다(전부 읽기 전용 조회 + draft 전용 쓰기만).
- **판단이 필요 없는 숫자(kpi/detail/ytd)는 애초에 에이전트 도구로 노출하지 않는다** — 고정 집계 스크립트만 접근 가능.
- **승인(draft→approved) 자체를 에이전트 도구로 만들지 않는다** — 에이전트가 아무리 판단을 잘못해도 확정된 리포트를 스스로 만들어낼 방법이 없게 한다.
- 메일·메신저 발송 도구는 MVP에 포함하지 않음 — "승인 후 사람이 직접 링크 공유"가 원칙.

---

## 6. 사람 개입(Human-in-the-loop) 지점

1. **실행 전**: 조회 기간/필터 확정 (트리거 자체가 사람의 입력)
2. **추가 조회 반복 한도 근접 시**: 3회 이상 재조회가 필요하다고 판단되면, 계속 진행할지 사람에게 확인(선택적 알림, MVP는 자동 진행 후 로그에 표시)
3. **최종 승인(필수, draft→approved)**: 리크루터가 앱에서 draft 인사이트(+ 항상 최신인 핵심지표 화면 숫자)를 함께 검토하고 승인 버튼을 눌러야만 서버가 DB 상태를 `approved`로 바꿈 — 이 순간부터 확정된 리포트로 노출됨. 에이전트는 이 상태를 스스로 바꿀 권한이 없음(5장, 3.2 참고)
4. **수정 요청**: 리크루터가 인사이트 내용을 지적하면 해당 draft만 재생성(전체 재실행 아님)

---

## 7. 관찰 가능성 (Observability)

- **실행 로그(trace)**: 각 도구 호출마다 "왜 호출했는지(에이전트의 판단 이유) → 무엇을 호출했는지(파라미터) → 무엇을 받았는지(결과 요약)"를 화면에 실시간 표시
- **비용/시간 기록**: 각 실행(run)마다 단계별 소요 시간, 토큰 사용량, 예상 비용을 기록해 병목 단계를 파악
- **결과물 직접 확인**: "완료"라고 표시되어도 리포트 미리보기 화면에서 사람이 실제 문서를 열어봐야 함 — 상태 표시와 실물 결과물을 분리하지 않음

---

## 8. 핵심 기능(MVP)

1. 기간(및 선택적 부서/직무) 선택 → 리포트 생성 트리거
2. 에이전트 루프 실행: 조회 → 관리도 기반 이상치 판단 → 추가 조회 → 인사이트 작성 → DB에 draft 저장
3. 실시간 실행 로그 화면 (도구 호출 이유/파라미터/결과)
4. draft 인사이트 + 핵심지표 화면(항상 최신)을 함께 보여주는 리포트 미리보기
5. 승인(draft→approved) / 수정요청 액션
6. 실행 이력 목록 (과거 run, 소요시간·비용 기록)
7. 핵심지표 화면 (kpi/detail/ytd를 DB에서 매번 다시 계산해 표로 표시, 판단 없음)

**MVP 제외(향후 확장)**: 자동 이메일 발송, 다중 에이전트(조사/작성/검토 역할 분리), 스케줄 자동 트리거, Slack 알림 연동

---

## 9. 화면 구성 (Screens)

| 화면 | 내용 |
|---|---|
| 대시보드 | 기간/필터 선택, "리포트 생성" 버튼, 최근 리포트 목록(상태: 생성중/검토대기/승인완료) |
| 실행 로그 | 현재 실행 중인 run의 단계별 trace 실시간 스트리밍 |
| 리포트 미리보기 | DB의 draft 인사이트 텍스트 + 핵심지표 화면 링크 함께 표시, "수정 요청" 버튼 |
| 승인 화면 | 최종 승인 버튼(DB draft→approved 상태 변경), 승인 이력 |
| 실행 이력/비용 대시보드 | run별 소요시간, 토큰, 비용, 성공/실패 여부 |
| 핵심지표 | kpi/detail/ytd를 DB에서 매번 다시 계산해 표로 표시(판단 없음, `/metrics`) |

### 9.1 리포트 레이아웃 참고 (숫자 부분은 표로 단순화)

사용자가 제공한 사내 "Data Driven Recruiting Dashboard" 사례(도넛/퍼널 차트)를 원래 기준으로 삼았으나,
Looker Studio 대시보드를 사람이 손으로 디자인해야 하는 비용 대비 이득이 낮다고 판단해(3.1 변경 이력 참고)
**숫자 부분은 핵심지표 화면의 표로 단순화**했다. 아래 레이아웃은 "논리적으로 어떤 숫자가 어디 있는지"의
기준으로만 남겨두고, 실제 화면은 도넛/퍼널 그림이 아니라 표(9번 화면)다. 인사이트 텍스트 박스는 표와
별개로 리포트 미리보기 화면에서 DB의 draft/approved 값을 그대로 보여준다.

```
┌─────────────────────────────────────────────────────────────┐
│  [기간] Recruiting Dashboard                                  │
├───────────────┬───────────────┬───────────────┬───────────────┤
│ Total Applicant│  Self-Drop    │   Rejected    │  In-Progress  │  ← KPI 카드 4개
├───────────────┴───────────────┴───────────────┴───────────────┤
│  Application → SC Stage → Interview → Reference Check          │
│  → Offer Sent → Offer Signed                (퍼널 플로우, 인원수)│  ← Sheets tab: detail (category=퍼널)
├───────────────────────┬───────────────────────┬─────────────────┤
│ Total Applicant       │ 지원경로              │ 합격자 지원경로   │
│ Analytics (부서별 도넛)│ (전체 지원자 채널 도넛) │ (합격자만 채널 도넛)│  ← Sheets tab: detail (category=부서/채널)
├───────────────────────┴───────────────────────┴─────────────────┤
│ In-Progress Ratio (진행중 인원의 현재 단계 분포 도넛)              │
├───────────────┬───────────────┬───────────────┬───────────────┬─┤
│ Joined(YTD)   │ Hired(YTD)    │ AVG Date to   │ AVG date to   │ │  ← YTD KPI 카드
│               │               │ Join(YTD)     │ hire(YTD)     │ │     (부서 필터 시 "En Joined" 처럼
│               │               │               │               │ │      "[부서약어] " 접두 카드 추가)
└─────────────────────────────────────────────────────────────┘
   ▲ 여기까지가 핵심지표 화면이 표로 보여주는 범위(도넛/퍼널 그림 아님, 9.1 참고).
     아래 인사이트는 이 화면 안이 아니라 우리 앱의 리포트 미리보기 화면에 별도로 표시된다 (3.2 참고).

┌─────────────────────────────────────────────────────────────┐
│  📝 인사이트 (에이전트 작성 → DB에 draft 저장 → 사람 승인)         │
│  - 이번 기간 주목할 지표와 그 이유                                │
│  - 원인 분석(추가 조회 결과 인용)                                 │
│  - 제안하는 다음 조치                                            │
└─────────────────────────────────────────────────────────────┘
```

- KPI 카드 4개는 `applicant_status_counts` 쿼리 → 핵심지표 화면의 "월별 핵심지표" 표.
- 퍼널 수치, 부서별/채널별 분포(전체 지원자·합격자만), In-Progress 비율은 각각 `funnel_events` / `department_stats` / `channel_stats(scope=all_applicants|hired_only)` / `in_progress_stage_ratio` 쿼리로 얻어 핵심지표 화면의 각 표에 표시. 지원경로 vs 합격자 지원경로의 **차이 자체**가 에이전트의 대표적인 이상치 탐지 소재다. 예: "지원경로에서는 Direct Sourcing 비중이 크지 않은데, 합격자 지원경로에서는 비중이 급격히 커짐 → 채널별 합격률 격차"를 자동으로 언급하도록 설계(실제로 2025-11 합성 데이터에 이 패턴을 주입해뒀음, 4.2.1/README 참고).
- YTD 카드는 `ytd_summary` 쿼리 → 핵심지표 화면의 "연간누적지표" 표. 부서 필터가 걸리면 참고 사례처럼 "En Joined(YTD)" 식으로 부서 접두 카드를 추가 생성.
- 핵심지표 화면은 요청마다 DB를 다시 조회해서 그리므로(캐시 없음) 별도 배포나 갱신 스케줄 없이 항상 최신 상태다.

---

## 10. 평가(Evaluation) 설계

- **평가 세트**: 과거 N개월(예: 6개월) 실제 또는 합성 채용 데이터 스냅샷 + 각 스냅샷에 대해 사람이 미리 작성한 "정답 리포트"(golden report)
- **측정 지표**:
  - 핵심 지표 누락률: 정답 리포트에 있는 핵심 수치 중 에이전트 리포트가 놓친 비율
  - 환각(hallucination) 비율: 데이터로 뒷받침되지 않는 서술이 포함된 비율
  - 작업 완료율: 사람 개입 없이 초안까지 도달한 비율
  - 소요 시간 / 실행당 토큰 비용
  - 반복(재조회) 횟수 — 너무 적으면 원인 분석 부실, 너무 많으면 비효율
- **실패 케이스 분류**: 잘못된 도구 선택 / 중간 포기 / 환각된 인사이트 / 불필요한 반복 — 각 실패를 어느 단계(조회/이상치판단/draft저장)에서 발생했는지 기록
- **비교표**: 프롬프트·도구 설명·모델·단계 구성을 바꿔가며 위 지표 변화를 표로 기록

### 10.1 구현 (`eval/`)

- `eval/cases.py` — 골든 케이스 6개: `anomaly_log.json`에 실제로 주입해둔 이상치 2건(2026-03 오퍼수락률
  급락, 2025-11 Direct Sourcing 채널 급등) + 정상 기간 4개(오탐 확인용). 각 케이스는 기대 결과(이상치
  있음/없음)와 "정답이라면 반드시 언급해야 할 키워드"를 정의.
- `eval/run_eval.py` — 케이스별로 이미 완료된 run이 있으면 재사용(비용 절약), 없으면 `run_agent()`를
  새로 호출. 완료 여부, 정답 여부(골든 키워드 포함 / 오탐 표현 없음), **환각 의심**(원인분석 텍스트에
  등장하는 숫자가 실제 도구 호출 결과에 없으면 표시), 재조회 횟수·소요시간·토큰·추정비용을 표로 출력하고
  `eval/results/`에 CSV로 저장(설정 비교용 `--label` 태그 가능).

### 10.2 첫 실행 결과 (2026-09-09, 모델: gpt-4o-mini, baseline)

| case_id | 완료 | 정답 | 재조회 | 소요(s) | 비용($) | 비고 |
|---|---|---|---|---|---|---|
| anomaly_offer_signed_drop (2026-03) | ✅ | ❌ | 1 | 15.7 | 0.0040 | "특이사항 없음"으로 결론 — 골든 키워드 누락 |
| anomaly_channel_direct_sourcing_spike (2025-11) | ✅ | ❌ | 5 | 22.2 | 0.0076 | 퍼널만 조사, 채널 데이터 미조회 — 골든 키워드 누락 |
| normal_2025_05 | ✅ | ✅ | 3 | 13.3 | 0.0048 | |
| normal_2025_07 | ✅ | ✅ | 2 | 11.6 | 0.0045 | |
| normal_2025_09 | ✅ | ✅ | 3 | 17.5 | 0.0112 | |
| normal_2026_01 | ✅ | ✅ | 2 | 12.3 | 0.0054 | |

**요약**: 작업 완료율 6/6(100%), 정확도 4/6(67%), 평균 재조회 2.7회, 총 비용 $0.0375, 환각 의심 없음(숫자
전부 실제 조회 결과에서 인용).

**가장 중요한 발견 — 비결정성(non-determinism)**: 2026-03 케이스는 이전 수동 테스트(4.2.1 검증, 웹앱
브라우저 테스트)에서는 **정확히** 잡아냈던 이상치인데, 이번 평가 실행에서는 재조회를 1번만 하고 바로
"특이사항 없음"으로 결론 내렸다. 같은 입력(같은 기간)인데도 LLM 샘플링에 따라 결과가 달라진다는 뜻 —
**단일 실행으로는 에이전트 품질을 신뢰성 있게 측정할 수 없다.** 다음 개선 방향:
- 케이스당 n회(예: 3~5회) 반복 실행 후 다수결/최댓값으로 정확도 집계
- 시스템 프롬프트에 "이상치가 안 보여도 최소 N회는 다른 축(부서/채널/단계)으로 조회해본 뒤에만
  '특이사항 없음'이라고 결론 내리라"는 지시 추가해서 조기 포기를 줄이는 실험
- `temperature` 파라미터를 낮춰(현재 미지정=기본값) 결과 일관성이 개선되는지 비교

2025-11(채널 이상치)은 두 번의 독립 실행 모두에서 놓쳤다 — 우연이 아니라 "퍼널부터 보고 끝내는" 전략적
편향으로 보인다(README 참고). 프롬프트에 "채널·부서 축도 최소 1번은 확인하라"는 지시를 추가하는 실험이
다음 우선순위.

### 10.3 설정 비교 — baseline vs 프롬프트 개선(`check_axis`), 같은 골든 케이스 6개

10.2의 "다음 우선순위"를 바로 실험했다. `app/agent_loop.py`의 `PROMPT_VARIANTS["check_axis"]`는
"특이사항 없음이라고 결론 내리기 전에 department 또는 channel(특히 hired_only) 축을 최소 1번은 더
조회하라"는 지시를 baseline 프롬프트에 추가한 것. `python -m eval.run_eval --prompt-variant check_axis`로
같은 6개 케이스를 다시 실행(항상 새로 호출 — 캐시 재사용 안 함, PRD 10.1 참고).

| 지표 | baseline | check_axis |
|---|---|---|
| 완료율 | 6/6 (100%) | 6/6 (100%) |
| 정확도 | 4/6 (67%) | 4/6 (67%) — **동일** |
| 평균 재조회 횟수 | 2.7 | 3.8 (+41%) |
| 총 비용 | $0.0375 | $0.0363 |
| 환각 의심 | 0건 | 2건 (normal_2025_05, anomaly_channel_...) |

**정확도는 그대로인데, 그 이유가 흥미롭다.** 2025-11 케이스를 다시 들여다보니 — 프롬프트 지시는 실제로
먹혔다: 이번엔 에이전트가 진짜로 `channel` 메트릭을 `scope=hired_only`로 조회했다(원했던 행동을 정확히
함). 그런데 최종 결론에서는 그 데이터를 쓰지 않고, 대신 `run_chart_code`에 서로 다른 지표(진행중/불합격/
자진포기/총지원자 수)를 하나의 시계열인 것처럼 섞어 넣어 계산한 가짜 통계로 "총지원자 수가 이상치"라는
**엉뚱한 결론**을 내렸다. 즉:
- 지시를 따르는 것(도구를 올바르게 호출하는 것)과 그 결과를 올바르게 종합하는 것은 별개의 능력이고,
  **프롬프트로 전자는 쉽게 고쳐지지만 후자는 안 고쳐졌다.**
- `run_chart_code`가 입력값의 "의미"를 검증하지 않고 숫자만 받는다는 점(4.2.1에서 이미 알려진 취약점)이
  여기서도 새로운 오답의 원인이 됐다 — 다른 시나리오에서 같은 근본 원인이 반복해서 문제를 일으키고 있다.
- 재조회 횟수가 늘었는데도(더 많이 찾아봤는데도) 정확도가 그대로라는 건, **"더 많이 조회하게 만들기"가
  이 문제의 해법이 아니라는 뜻** — 다음 실험은 프롬프트보다 `run_chart_code` 자체를 손보는 쪽(예: 입력이
  같은 metric/stage에서 나온 값들인지 확인하는 파라미터 추가)이 더 유망해 보인다.

두 결과 CSV 원본: `eval/results/*_baseline.csv`, `eval/results/*_check_axis.csv`.

---

## 11. 배포 (요약)

- **우리 앱**: 웹앱 형태로 단독 배포, URL로 접속해 기간 선택 → 리포트 생성 → 로그 확인 → draft 미리보기 → 승인 → 핵심지표 확인까지 전 과정 실행 가능해야 함. 외부 계정(Google 등) 없이 이 앱 하나로 자체완결.
- `OPENAI_API_KEY`, DB 경로 등 민감정보는 환경변수로 관리. README에 실행 방법 정리
- (선택) Google Sheets/Looker Studio 연동을 켜고 싶으면 `GOOGLE_SERVICE_ACCOUNT_JSON`/`REPORT_SHEET_ID`를 추가로 설정 — 필수 아님(3.1 참고)
- **수용 기준(안)**: 평가 세트 시나리오 N개 중 사람 개입(수정 요청) 없이 승인까지 도달한 것이 M개 이상 (예: N=10, M=7)

---

## 12. 오픈 이슈 / 다음 결정 필요 사항

- [ ] 실제 채용 데이터 소스 확정 (사내 ATS 연동 vs 합성 데이터로 시작)
- [x] ~~이상치 탐지 방법~~ → 관리도(Control Chart, μ±2σ) 방식으로 확정 (4.2.1)
- [x] ~~차트/문서 생성 방식~~ → v0.3~v0.4에서는 Looker Studio+Google Sheets였으나, v0.5에서 **핵심지표 화면(표, 앱 내장)**으로 최종 확정 — 대시보드 수작업이 과제 채점 기준과 무관한 순수 노동이라는 사용자 판단 (3.1, 9.1)
- [x] ~~인사이트 저장 위치~~ → Sheets가 아니라 우리 앱 DB(`insight_drafts`)로 확정 — "에이전트가 필요한 일만 하도록" 범위를 좁히면서, 승인 게이트도 Google API 권한 분리 대신 DB 상태값(draft/approved)으로 단순화 (3.2, 5장)
- [x] ~~Looker Studio 대시보드 디자인/임베드/Sheets 인증 방식~~ → 필수 경로에서 제외되며 해소. Sheets 연동은 `app/sheets_writer.py`에 선택 기능으로만 남음
- [ ] 도넛/퍼널 같은 실제 차트가 필요해지면 어떤 방식으로 그릴지(핵심지표 화면에 JS 차트 라이브러리 추가 vs 그때 가서 Looker Studio 재검토)
- [x] ~~에이전트가 "분석은 다 해놓고 저장을 안 하는" 실패~~ → nudge 로직(MAX_NUDGES=2)으로 대응, 재현 시나리오 재실행으로 확인 완료(README)
- [x] ~~중간에 끊겨도 이어서 재개 가능해야 함~~ → `resume_run()` 구현·검증 완료 (4.4, `tests/test_resume.py`)
- [ ] **`run_chart_code`가 입력값 시리즈의 의미(같은 metric/stage에서 나온 값들인지)를 검증하지 않아
      엉뚱한 숫자를 섞어 넣어도 그럴듯한 통계를 돌려주는 문제** — 10.3 비교 실험에서 새로 드러난 근본
      원인. 프롬프트 지시보다 이 도구 자체를 손보는 게 다음 우선순위로 보임(예: 같은 조회에서 나온
      값들인지 taging해서 검증)
- [ ] 비결정성 완화: 케이스당 n회 반복 실행 + 다수결 채점, `temperature` 조정 실험 (10.2 참고)
- [ ] 스택 결정: Claude Agent SDK 단일 에이전트로 시작 후 역할 분리(조사/작성/검토) 확장 여부
